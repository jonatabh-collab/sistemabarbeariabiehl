{
  "name": "barbearia-biehl",
  "version": "1.0.0",
  "description": "Sistema completo de gestão para Barbearia Biehl",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "admin": "cd admin && npm run dev",
    "client": "cd client && npm run dev -- --port 5174",
    "install:all": "cd backend && npm install && cd ../admin && npm install && cd ../client && npm install",
    "dev": "concurrently --names \"API,ADMIN,CLIENT\" --prefix-colors \"cyan,green,yellow\" \"npm run backend\" \"npm run admin\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
